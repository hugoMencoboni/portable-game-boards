import leMurmureDesFeuilles from "./le-murmure-des-feuilles";
import nextStationLondon from "./next-station-london";
import { Game } from "./types";

const games: Game[] = [leMurmureDesFeuilles, nextStationLondon];

export default games;
